#!/usr/bin/env perl
use FindBin;
select((select(STDOUT),$| = 1)[0]);
select((select(STDERR),$| = 1)[0]);
while(1) {
    my $cmd = <STDIN>;
    exit if ($cmd =~/stop/);
    system( "perl -I$FindBin::Bin/../lib/perl5 $FindBin::Bin/exiftool $cmd");
    print "OK\n";
}
